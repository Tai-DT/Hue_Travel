# 2026-03-17

- User asked to rerun the Huế Travel project and explicitly requested spawning a subagent to explore the repo.
- Loaded required startup context from `SOUL.md`, `USER.md`, and `memory/2026-03-16.md`; `BOOTSTRAP.md`, `MEMORY.md`, and `memory/2026-03-17.md` were initially absent.
- Confirmed local infra was already healthy via `docker compose ps` and ensured it stayed up with `docker compose up -d`.
- Restarted local runtime sessions for:
  - API via `cd apps/api && go run ./cmd/server`
  - Admin via `cd apps/web && npm run dev`
  - Provider portal via `cd apps/provider-portal && npm run dev`
  - Mobile Expo via `cd apps/mobile && REACT_NATIVE_PACKAGER_HOSTNAME=192.168.1.246 npx expo start --port 8081`
- Verified live endpoints after restart:
  - API health: `http://localhost:8080/health`
  - Admin: `http://localhost:3000`
  - Provider portal: `http://localhost:3001`
  - Expo/Metro: `http://localhost:8081`
- Verified listeners on ports `8080`, `3000`, `3001`, and `8081`.
- Explorer subagent summary: monorepo with Go API, Expo mobile, Next admin, and Next provider portal; canonical startup is documented in `README.md`, `docs/SETUP.md`, `Makefile`, and per-app `package.json` files.
- Added local auth system so Expo/mobile no longer depends on Google as the primary login path.
- Backend changes:
  - Added `POST /api/v1/auth/register` and `POST /api/v1/auth/login` in `apps/api/internal/app/routes.go`.
  - Implemented password-based registration/login in `apps/api/internal/service/services.go` using `bcrypt`.
  - Updated `apps/api/internal/handler/auth.go` and `apps/api/internal/handler/docs.go` to expose/document the new auth endpoints.
  - Updated `apps/api/internal/repository/user.go` to load `password_hash` for auth flows.
  - Added auth service tests in `apps/api/internal/service/auth_service_test.go` and mock phone lookup normalization in `apps/api/internal/testutil/mocks.go`.
- Mobile Expo changes:
  - Added `register` and `loginWithPassword` methods in `apps/mobile/src/services/api.ts`.
  - Rebuilt `apps/mobile/src/screens/LoginScreen.tsx` so the primary flow is now local email/phone + password, OTP remains fallback, and Google remains optional.
- Added password setup/change flow for existing users:
  - Added `POST /api/v1/auth/password` in `apps/api/internal/app/routes.go`.
  - Implemented password set/rotate logic in `apps/api/internal/service/services.go`.
  - Exposed the endpoint in `apps/api/internal/handler/auth.go` and documented it in `apps/api/internal/handler/docs.go`.
  - Added password management UI in:
    - `apps/mobile/src/screens/SettingsScreen.tsx`
    - `apps/web/src/app/settings/page.tsx`
    - `apps/provider-portal/src/app/profile/page.tsx`
  - Connected local password login to:
    - `apps/web/src/components/LoginPage.tsx`
    - `apps/provider-portal/src/components/GuideLoginPage.tsx`
    - `apps/web/src/lib/api.ts`
    - `apps/provider-portal/src/lib/api.ts`
- Fixed legacy placeholder password hashes:
  - Added `apps/api/internal/model/password.go` so only valid bcrypt hashes count as “has password”.
  - Updated repository/service/mock code to treat dummy seed hashes as no-password accounts.
  - Updated `scripts/seed_real_data.sql` to stop seeding fake bcrypt placeholders.
- Simplified auth to email/password only:
  - Removed exposed auth routes for OTP and Google from `apps/api/internal/app/routes.go`.
  - Updated auth contract in `apps/api/internal/service/services.go` so register/login accept email only.
  - Removed OTP/Google login UI and client methods from:
    - `apps/mobile/src/screens/LoginScreen.tsx`
    - `apps/mobile/src/services/api.ts`
    - `apps/web/src/components/LoginPage.tsx`
    - `apps/web/src/lib/api.ts`
    - `apps/provider-portal/src/components/GuideLoginPage.tsx`
    - `apps/provider-portal/src/lib/api.ts`
  - Updated docs in `apps/api/internal/handler/docs.go` and `apps/api/docs/openapi.yaml`.
- Verification:
  - `go test ./...` passed in `apps/api`.
  - `npx expo export --platform web` passed in `apps/mobile`.
  - `npm run build` passed in `apps/web`.
  - `npm run build` passed in `apps/provider-portal`.
  - Live API verification passed for both `POST /api/v1/auth/register` and `POST /api/v1/auth/login`.
  - Live API verification passed for legacy-account migration flows:
    - `admin@huetravel.vn` via OTP login -> `has_password=false` -> `POST /api/v1/auth/password` -> password login success
    - `lan.tran@hue.vn` via OTP login -> `has_password=false` -> `POST /api/v1/auth/password` -> password login success
  - Live API verification after auth simplification:
    - `POST /api/v1/auth/otp/send` -> `404`
    - `POST /api/v1/auth/google` -> `404`
    - `POST /api/v1/auth/login` with legacy `identifier` payload -> `400`
    - `POST /api/v1/auth/login` with `{email,password}` -> `200`
    - `POST /api/v1/auth/register` with email/password -> `201`
- Deep cleanup after removing OTP/Google auth:
  - Deleted backend dead code for OTP/Google/SMS auth:
    - removed `apps/api/internal/service/sms.go`
    - removed OTP model/repository/service wiring and ESMS config usage from `apps/api/internal/config/config.go`, `apps/api/internal/model/models.go`, and `apps/api/internal/repository/user.go`
  - Updated local dev seeds to ship with working password hashes (`HueTravel123!`) in:
    - `scripts/seed.sql`
    - `scripts/seed_real_data.sql`
  - Rewrote smoke scripts to use email/password auth instead of OTP:
    - `scripts/smoke-auth.sh`
    - `scripts/smoke-booking.sh`
  - Cleaned docs/config leftovers in:
    - `README.md`
    - `docs/API.md`
    - `docs/SETUP.md`
    - `docs/DEPLOY.md`
    - `.env.example`
    - `deploy/docker-compose.prod.yml`
    - `apps/web/src/app/settings/page.tsx`
  - Verification:
    - `go test ./...` passed again in `apps/api`
    - `npm run build` passed again in `apps/web`
    - `./scripts/smoke-auth.sh` passed with auto-registered local account
    - `./scripts/smoke-booking.sh` passed with auto-registered traveler account
    - repo-wide leftover search only still found OTP artifacts in historical migration `apps/api/migrations/001_initial_schema.sql`, intentionally kept for schema compatibility
- User asked whether the project's functions are fully completed.
- Re-verified current repo state:
  - `docker compose ps` showed PostgreSQL, Redis, MinIO, and Meilisearch healthy.
  - `go test ./...` passed in `apps/api`.
  - `npm run build` passed in `apps/web`.
  - `npm run build` passed in `apps/provider-portal`.
  - `npx expo export --platform web` passed in `apps/mobile`.
  - Live API health check returned healthy on `http://localhost:8080/health`.
  - `./scripts/smoke-auth.sh` passed.
  - `./scripts/smoke-booking.sh` passed and confirmed booking transitions after mock payment.
- Current assessment:
  - Core product flows are implemented and currently working locally across API, mobile, admin, and provider portal.
  - Backend route table includes broad coverage for auth, experiences, bookings, guides, chat, AI, search, admin, friends, trips, calls, weather, promotions, gamification, blog, diary, events, emergency, feed, translation, and collections.
  - The app is not yet “fully complete” in a strict production sense because several external integrations still support dev fallback/mock behavior when credentials/services are absent, including AI, places/maps, search, payment, uploads, and some admin stats fallback paths.
  - Repo remains dirty with many uncommitted changes, indicating ongoing work rather than a finalized clean release state.
- User agreed to continue with a production-readiness audit focused on fallback/mock behavior before deploy.
- Verified production/mock wiring details:
  - `cfg.Validate()` is called from `apps/api/cmd/server/main.go`, so strict-mode env validation does run on startup.
  - Found a real gap: `ALLOW_MOCK_SERVICES` was loaded into config but not actually used to construct AI/places/payment/search/upload services.
  - Found deploy drift:
    - `apps/api/Dockerfile` health check used `/api/v1/health` even though the live endpoint is `/health`
    - `deploy/docker-compose.prod.yml` was missing `OPENWEATHER_API_KEY`
    - `deploy/.env.example` still contained obsolete ESMS variables and did not expose weather config
    - `docs/DEPLOY.md` used the wrong API health endpoint path
- Applied safe fixes:
  - Wired `cfg.App.AllowMockServices` into service construction in `apps/api/internal/app/container.go` for AI, places, payment, search, and upload services.
  - Fixed API Docker health check path in `apps/api/Dockerfile`.
  - Added `OPENWEATHER_API_KEY` to `deploy/docker-compose.prod.yml`.
  - Updated `deploy/.env.example` to remove stale ESMS values and document AI/maps/weather keys.
  - Updated `docs/DEPLOY.md` to include weather config and the correct `/health` endpoint.
- Verification after fixes:
  - `go test ./...` passed again in `apps/api`.
- Created deploy audit document:
  - `docs/PRODUCTION_BLOCKERS.md`
- Remaining blockers called out in that doc:
  - Weather service still returns mock data on missing key or upstream failure and is not controlled by `ALLOW_MOCK_SERVICES`.
  - Notification push silently degrades without `FCM_SERVER_KEY`.
  - Notification handler still falls back to mock data if DB queries fail.
  - AI quick-suggest still has a static fallback path and needs a product decision for production behavior.
- User asked to ensure login does not go through a third party.
- Re-verified current auth state:
  - Mobile login screen already uses internal email/password only in `apps/mobile/src/screens/LoginScreen.tsx`.
  - Admin login already uses internal email/password only in `apps/web/src/components/LoginPage.tsx`.
  - Provider portal login already uses internal email/password only in `apps/provider-portal/src/components/GuideLoginPage.tsx`.
  - Backend no longer exposes public OTP/Google auth routes in `apps/api/internal/app/routes.go`.
- Cleaned remaining third-party auth leftovers:
  - Removed Google OAuth env placeholders from `.env.example`.
  - Removed unused `expo-auth-session` from `apps/mobile/package.json` and `apps/mobile/package-lock.json`.
- Verification:
  - `npm ls expo-auth-session` now returns empty in `apps/mobile`.
  - `npx expo export --platform web` still passes in `apps/mobile`.
- Continued cleanup after user confirmed:
  - Removed lingering Google Expo vars from `apps/mobile/.env`, so Expo no longer loads any Google auth client IDs.
  - Removed Google auth placeholders from `apps/mobile/.env.example`, `apps/web/.env.example`, and `apps/provider-portal/.env.example`.
  - Removed unused `expo-web-browser` from `apps/mobile/package.json` and `apps/mobile/package-lock.json`.
  - Removed dead i18n keys `auth.loginWithGoogle` and `settings.connectedGoogle` from all mobile locale files.
- Final verification:
  - `rg -n --hidden "NEXT_PUBLIC_GOOGLE_CLIENT_ID|EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID|EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID|EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID|GOOGLE_CLIENT_IDS|loginWithGoogle|connectedGoogle|expo-auth-session|expo-web-browser" . -g '!**/node_modules/**'` only matches historical notes in `memory/`.
  - `npm ls expo-web-browser expo-auth-session` returns empty in `apps/mobile`.
  - `npx expo export --platform web` passes and Expo now exports only `EXPO_PUBLIC_API_URL`.
- Re-checked current local readiness with the project running:
  - `go test ./...` passes in `apps/api`.
  - `npm run build` passes in `apps/web`.
  - `npm run build` passes in `apps/provider-portal`.
  - `npx expo export --platform web` passes in `apps/mobile`.
  - `./scripts/smoke-api.sh` passes against `http://localhost:8080`.
  - `./scripts/smoke-auth.sh` passes for email/password register -> login -> refresh -> logout.
  - `./scripts/smoke-booking.sh` passes for register -> booking -> payment create -> confirmed booking status -> logout.
- Current conclusion reaffirmed:
  - Core local flows are working.
  - The repo is not yet fully "all features completed" in a production sense because many secondary modules are only confirmed via route/code/build presence, and some external-service fallback paths remain documented in `docs/PRODUCTION_BLOCKERS.md`.
- Created a clearer feature-by-feature matrix:
  - Added `docs/FEATURE_COMPLETION_MATRIX.md`
  - Matrix classifies each feature group as `Verified`, `Present`, or `Blocked`
  - It also separates local status from production readiness so future audits can update one document instead of repeating a full repo scan
- Added an action-oriented release checklist:
  - `docs/RELEASE_CHECKLIST.md`
  - Splits status into `Đã xong`, `Cần test thêm`, and `Phải sửa trước deploy`
  - Includes a priority order so future work can start from production blockers instead of broad re-audits
- Implemented the first remaining production blocker: weather fail-fast behavior
  - `apps/api/internal/service/weather.go` now supports `fallbackEnabled` and returns `ErrServiceNotConfigured` / `ErrServiceUnavailable` when mock fallback is disabled
  - `apps/api/internal/app/container.go` now wires weather through `cfg.App.AllowMockServices`
  - `apps/api/internal/handler/weather.go` now returns `503 Service Unavailable` for unavailable/unconfigured weather in strict mode
  - `apps/api/internal/config/config.go` now requires `OPENWEATHER_API_KEY` in strict mode
  - Added tests:
    - `apps/api/internal/service/weather_test.go`
    - `apps/api/internal/config/config_test.go`
    - `apps/api/internal/handler/weather_test.go`
- Verification after weather fix:
  - `go test ./internal/service ./internal/config ./internal/handler` passed
  - `go test ./...` passed in `apps/api`
  - Restarted local API server with the new code
  - `curl http://localhost:8080/api/v1/weather/current` still returns success in dev mode with mock weather
  - `curl http://localhost:8080/api/v1/weather/forecast` still returns success in dev mode with mock forecast
  - `./scripts/smoke-api.sh` passed after restart
- Updated release docs to reflect the fix:
  - `docs/PRODUCTION_BLOCKERS.md`
  - `docs/FEATURE_COMPLETION_MATRIX.md`
  - `docs/RELEASE_CHECKLIST.md`
- Implemented the next notification-related production fixes
  - `apps/api/internal/config/config.go` now requires `FCM_SERVER_KEY` in strict mode
  - `apps/api/internal/service/notification.go` now treats blank/whitespace FCM keys as not configured and logs more explicitly in local/dev
  - `apps/api/internal/handler/notification.go` now only uses mock fallback when `ALLOW_MOCK_SERVICES=true`
  - When `ALLOW_MOCK_SERVICES=false`, notification endpoints return `503 Service Unavailable` instead of silent mock/zero responses
  - `apps/api/internal/app/container.go` now passes `cfg.App.AllowMockServices` into `NewNotificationHandler`
- Added tests for notification/config changes:
  - extended `apps/api/internal/config/config_test.go`
  - added `apps/api/internal/handler/notification_test.go`
  - adjusted `apps/api/internal/handler/e2e_test.go` for the updated notification handler constructor
- Verification after notification fix:
  - `go test ./internal/config ./internal/handler` passed
  - `go test ./...` passed in `apps/api`
  - Restarted local API server again with the new code
  - `./scripts/smoke-api.sh` passed after restart
- Updated docs again:
  - `docs/PRODUCTION_BLOCKERS.md`
  - `docs/FEATURE_COMPLETION_MATRIX.md`
  - `docs/RELEASE_CHECKLIST.md`
  - Push config and notification fallback are no longer listed as remaining production blockers there
- Implemented the next AI production-safety fix: quick suggest no longer silently falls back in strict mode
  - `apps/api/internal/service/ai_trip_planner.go` now exposes `FallbackEnabled()`
  - `apps/api/internal/handler/ai.go` now returns `503 Service Unavailable` for quick suggest when `ALLOW_MOCK_SERVICES=false` and Gemini is unavailable, unconfigured, or returns invalid JSON
  - static quick suggest remains available only for local/dev fallback mode
  - added `apps/api/internal/handler/ai_test.go`
- Verification after AI quick suggest fix:
  - `go test ./internal/handler ./internal/service` passed in `apps/api`
  - `go test ./...` passed in `apps/api`
  - restarted the local API server with the updated code
  - `./scripts/smoke-api.sh` passed after restart
  - `curl http://localhost:8080/api/v1/ai/suggest?type=general` returned `200` in dev mode with `source=static`, confirming local fallback still works
- Updated docs again to reflect the new AI state:
  - `docs/PRODUCTION_BLOCKERS.md`
  - `docs/FEATURE_COMPLETION_MATRIX.md`
  - `docs/RELEASE_CHECKLIST.md`
  - AI Quick Suggest is no longer listed as a silent-fallback blocker; remaining work is now framed as real integration/UAT for VNPay, Gemini, search, upload, and maps
- Tightened VNPay production config safety:
  - `apps/api/internal/config/config.go` now requires `VNPAY_RETURN_URL` in strict mode and rejects the local default callback URL
  - added `TestValidateRequiresVNPayReturnURLInStrictMode` in `apps/api/internal/config/config_test.go`
  - updated `deploy/.env.example`, `deploy/docker-compose.prod.yml`, and `docs/DEPLOY.md` to include `VNPAY_RETURN_URL`
  - updated `docs/PRODUCTION_BLOCKERS.md` and `docs/RELEASE_CHECKLIST.md` so VNPay blocker now explicitly includes the real callback URL requirement
- Verification after VNPay return URL fix:
  - `go test ./internal/config ./...` passed in `apps/api`
  - `./scripts/smoke-api.sh` passed against the already-running local API
- Fixed the search production path so it no longer relies on static/in-memory fallback outside dev:
  - `apps/api/internal/service/search.go`
    - `Suggest`, `Trending`, and `GetStats` now use Meilisearch in configured mode and return typed service errors in strict mode
    - added strict-mode tests for suggest/trending/stats
    - fixed startup sync query to use real `experiences` schema fields (`category::text`, `meeting_point`)
  - `apps/api/internal/handler/search.go`
    - now maps strict-mode search helper errors to `503 Service Unavailable`
  - `apps/api/internal/app/container.go`
    - now calls `SearchSvc.SyncFromDB(ctx, c.Pool)` during startup when PostgreSQL is available
  - added `apps/api/internal/handler/search_test.go`
- Verification after search fix:
  - `go test ./...` passed in `apps/api`
  - restarted the local API server with the updated code
  - startup logs now show successful search sync from PostgreSQL and push to Meilisearch
  - `./scripts/smoke-api.sh` passed after restart
  - `GET /api/v1/search`, `GET /api/v1/search/suggest`, `GET /api/v1/search/trending`, and `GET /api/v1/search/stats` all returned `200` on local runtime
- Updated docs again:
  - `docs/PRODUCTION_BLOCKERS.md`
  - `docs/FEATURE_COMPLETION_MATRIX.md`
  - `docs/RELEASE_CHECKLIST.md`
  - Search is no longer listed as a production fallback blocker; remaining blockers are now VNPay, AI upstream, upload, and maps/places
- Tightened upload production error handling and verified real local upload:
  - `apps/api/internal/service/file_upload.go` now wraps MinIO runtime failures as `ErrServiceUnavailable`
  - added strict-mode tests in `apps/api/internal/service/service_test.go`
  - added `apps/api/internal/handler/upload_test.go` to verify handlers return `503` when storage is unavailable in strict mode
- Verification after upload fix:
  - `go test ./internal/service ./internal/handler ./...` passed in `apps/api`
  - performed a real authenticated upload against `POST /api/v1/upload` on local API
  - upload succeeded and returned a real MinIO URL under `http://localhost:9000/hue-travel/...`
- Updated docs again:
  - `docs/PRODUCTION_BLOCKERS.md`
  - `docs/FEATURE_COMPLETION_MATRIX.md`
  - `docs/RELEASE_CHECKLIST.md`
  - Upload is no longer listed as a remaining production fallback blocker; the unresolved blocker list is now VNPay, AI upstream, and maps/places
- Tightened maps/places strict-mode behavior and verified real local upstream responses:
  - `apps/api/internal/service/goong_places.go` now returns typed `ErrServiceUnavailable` for directions status/no-route failures in strict mode
  - added `TestGoongPlacesService_StrictModeRequiresAPIKey` in `apps/api/internal/service/service_test.go`
  - added `apps/api/internal/handler/place_test.go` to verify search/nearby/directions return `503` when places service is unavailable in strict mode
- Verification after places/maps check:
  - `go test ./internal/service ./internal/handler ./...` passed in `apps/api`
  - local API returned real upstream data for:
    - `GET /api/v1/places/search?q=Đại Nội`
    - `GET /api/v1/places/nearby?...`
    - `GET /api/v1/places/directions?...`
- Updated docs again:
  - `docs/PRODUCTION_BLOCKERS.md`
  - `docs/FEATURE_COMPLETION_MATRIX.md`
  - `docs/RELEASE_CHECKLIST.md`
  - places/maps is no longer listed as a code-level production blocker; remaining unresolved blockers are now VNPay and AI upstream verification
