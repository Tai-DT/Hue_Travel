# 2026-03-15

- Loaded workspace session context from `SOUL.md`, `USER.md`, and recent memory before acting.
- Docker daemon was not running initially; started Docker Desktop and waited for the daemon before launching the local stack.
- Confirmed full local project stack is running: Docker infra, Go API, admin web, provider portal, and Expo web.
- Verified local endpoints respond on `http://localhost:8080/health`, `http://localhost:3000`, `http://localhost:3001`, and `http://localhost:19006`.
- API started in development mode with local mock-friendly config; ESMS is not configured, so OTP is logged locally instead of sent by SMS.
- Ran full backend smoke suite successfully: `smoke-stack`, `smoke-api`, `smoke-auth`, and `smoke-booking`.
- Ran `go test ./... -v` and `go test ./... -race` in `apps/api`; both passed.
- Built all frontends successfully in production mode: `apps/web`, `apps/provider-portal`, and Expo web export in `apps/mobile`.
- Browser-tested admin, provider portal, and mobile web flows.
- Admin finding: bookings list shows missing traveler/experience labels even though dashboard and provider/mobile screens have the data.
- Provider portal uses local seed account `0907778888` from `scripts/seed.sql`; older guide phones from `apps/api/migrations/002_seed_data.sql` are not present in the current local database.
- Mobile web booking + payment flow succeeded end-to-end for a new test booking on `2026-03-29`; booking was later cancelled for cleanup.
- Mobile profile logout was not fully verified because the browser automation session did not reliably handle the React Native Web confirmation dialog.
- Cleaned up temporary test changes where possible: reset booking `889df811...` back to `confirmed`, removed featured flag from review `730f2ff3...`, and soft-deleted temporary experience `UI Test Tour 20260315-1231`.
- Replaced login UI in admin web, provider portal, and mobile app from phone/OTP-first to Google-first.
- Backend Google auth now links existing accounts by matching verified Google email before creating a new traveler, so admin/guide roles are preserved when those users sign in with Gmail.
- Backend Google token audience validation now accepts multiple client IDs via `GOOGLE_CLIENT_IDS`, with `GOOGLE_CLIENT_ID` kept as a fallback.
- Found several critical files had been emptied to 0 bytes (`.env`, `.env.example`, `container.go`, `config.go`, `place.go`), which explained the compile break after an accidental delete/edit.
- Restored the emptied backend app/config/handler files and rebuilt the root `.env.example`; reconstructed `.env` as a safe local scaffold because the original secrets could not be recovered from disk state.
- Switched the backend places integration from Google Maps to Goong Maps by wiring a new `GoongPlacesService` into the API container and place handler.
- `GOONG_API_KEY` is now the primary map env var; `GOOGLE_MAPS_API_KEY` is accepted only as a temporary fallback during config load.
- Added a Goong REST implementation for autocomplete/place detail/directions and kept mock fallback behavior when no Goong API key is configured locally.
- Updated API docs text to reference Goong instead of Google Maps for places endpoints.
- Re-ran verification after the recovery and Goong migration: `go test ./...` in `apps/api`, `npm run build` in `apps/web`, `npm run build` in `apps/provider-portal`, and `npx expo export --platform web` in `apps/mobile`; all passed.
- Confirmed the local `.env` now contains a real `GOONG_API_KEY`, then validated live Goong responses through `/api/v1/places/search`, `/api/v1/places/nearby`, and `/api/v1/places/directions`.
- Standardized directions calls on `origin` / `destination` in `lat,lng` format while keeping backend backward compatibility with legacy `origin_lat` / `origin_lng` / `dest_lat` / `dest_lng`.
- Removed stale empty Google Maps config from `apps/mobile/app.json` because the current mobile map flow uses backend place APIs instead of native Google Maps SDK config.
- Cleaned remaining public docs/deploy references from Google Maps to Goong Maps, while leaving `GOOGLE_MAPS_API_KEY` pass-through in production compose for temporary backward compatibility.
