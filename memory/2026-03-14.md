# 2026-03-14

- Ran local stack successfully: Docker infra, Go API, admin web, provider portal, and Expo web.
- Fixed `apps/web` and `apps/provider-portal` npm scripts to allow `PORT` override while keeping existing defaults.
- Added static icons to both Next.js apps to remove runtime `404 /favicon.ico` errors.
- Normalized provider booking payloads in `apps/provider-portal/src/lib/api.ts` so UI works with nested `traveler.full_name` and `experience.title` from API responses.
- Verified provider dashboard now shows traveler and tour names correctly.
