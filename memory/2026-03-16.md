# 2026-03-16

- User asked to support logging in with phone number again.
- Confirmed mobile app still had OTP login, but admin web and provider portal had been changed to Google-only login UIs.
- Restored phone number + OTP login flows in [apps/web/src/components/LoginPage.tsx](/Volumes/FastVault-Apps/Projects/Desktop/Hue_Travel/apps/web/src/components/LoginPage.tsx) and [apps/provider-portal/src/components/GuideLoginPage.tsx](/Volumes/FastVault-Apps/Projects/Desktop/Hue_Travel/apps/provider-portal/src/components/GuideLoginPage.tsx), while keeping Google login as a secondary option.
- Added backend phone lookup normalization in [apps/api/internal/repository/phone.go](/Volumes/FastVault-Apps/Projects/Desktop/Hue_Travel/apps/api/internal/repository/phone.go) so `090...`, `84...`, and `+84...` can resolve to the same user when verifying OTP.
- Updated [apps/api/internal/repository/user.go](/Volumes/FastVault-Apps/Projects/Desktop/Hue_Travel/apps/api/internal/repository/user.go) to search phone candidates instead of exact string match only.
- Added unit coverage for phone candidate generation in [apps/api/internal/repository/phone_test.go](/Volumes/FastVault-Apps/Projects/Desktop/Hue_Travel/apps/api/internal/repository/phone_test.go).
- Verified backend with `go test ./...` in `apps/api`.
- Verified provider build with `npm run build` in `apps/provider-portal`.
- Admin build is still blocked by a pre-existing unrelated type error in `apps/web/src/app/guide-apps/page.tsx` (`res.data.applications` on `{}`), not by the login changes.
- Restarted local API/admin/provider/mobile dev servers after the auth changes.
- Verified live OTP login through backend:
  - `901111222` returned existing `admin` user with `is_new_user=false`
  - `+84907778888` resolved to existing seed guide `0907778888` and returned role `guide` with `is_new_user=false`
- Current local endpoints responding:
  - API `http://localhost:8080`
  - Admin `http://localhost:3000`
  - Provider `http://localhost:3001`
  - Expo web `http://localhost:8081`
- Fixed mobile Google login error `"Không thể xác thực Google. Thử lại sau."` by switching [apps/mobile/src/screens/LoginScreen.tsx](/Volumes/FastVault-Apps/Projects/Desktop/Hue_Travel/apps/mobile/src/screens/LoginScreen.tsx) from `useAuthRequest` to Expo's `useIdTokenAuthRequest`, which requests a real `id_token` on web instead of only an `access_token`.
- Removed the broken mobile fallback that tried to send a Google `access_token` to backend `/auth/google`; backend verifier only accepts an `id_token`.
- Added platform-aware Google client ID checks/messages in the mobile login screen so missing Android/iOS/web OAuth config is surfaced clearly.
- Verified the mobile app still builds with `npx expo export --platform web` and Expo dev server remains available on `http://localhost:8081`.
- Investigated mobile error text `"Không thể tải booking"` and traced it to [apps/mobile/src/screens/BookingScreen.tsx](/Volumes/FastVault-Apps/Projects/Desktop/Hue_Travel/apps/mobile/src/screens/BookingScreen.tsx), which treated a `success: true, data: null` response from `/api/v1/bookings` as a hard failure.
- Confirmed backend returned `data: null` for users with no traveler bookings, including admin and guide accounts, while travelers with bookings already got proper arrays.
- Fixed [apps/api/internal/repository/booking.go](/Volumes/FastVault-Apps/Projects/Desktop/Hue_Travel/apps/api/internal/repository/booking.go) to initialize empty booking slices so JSON serializes empty lists as `[]` instead of `null` for `ListByTraveler`, `ListByGuide`, and `ListAll`.
- Added a defensive mobile-side normalization in [apps/mobile/src/services/api.ts](/Volumes/FastVault-Apps/Projects/Desktop/Hue_Travel/apps/mobile/src/services/api.ts) so any legacy `null` booking payload is converted to `[]`.
- Restarted the local API and re-verified live: admin `/api/v1/bookings` now returns `{"success":true,"data":[]...}` while traveler accounts still receive real booking arrays.
- Verified again with `go test ./...` in `apps/api` and `npx expo export --platform web` in `apps/mobile`.
- Implemented first missing priority feature: `booking guide trực tiếp`.
- Added backend endpoint [apps/api/internal/app/routes.go](/Volumes/FastVault-Apps/Projects/Desktop/Hue_Travel/apps/api/internal/app/routes.go) `GET /api/v1/guides/:id/direct-booking`, backed by logic in [apps/api/internal/handler/review_favorite_guide.go](/Volumes/FastVault-Apps/Projects/Desktop/Hue_Travel/apps/api/internal/handler/review_favorite_guide.go) that finds or creates a reusable private booking experience for the selected guide.
- Chose the low-risk design of generating a dedicated `private guide experience` instead of changing booking schema to allow `experience_id = null`, so the existing booking/payment/review flow stays intact.
- Mobile home guide cards now use `guide.user_id` correctly and open the direct booking flow through the new API in [apps/mobile/src/services/api.ts](/Volumes/FastVault-Apps/Projects/Desktop/Hue_Travel/apps/mobile/src/services/api.ts), [apps/mobile/src/screens/HomeScreen.tsx](/Volumes/FastVault-Apps/Projects/Desktop/Hue_Travel/apps/mobile/src/screens/HomeScreen.tsx), and [apps/mobile/App.tsx](/Volumes/FastVault-Apps/Projects/Desktop/Hue_Travel/apps/mobile/App.tsx).
- Verified with `go test ./...`, `npx expo export --platform web`, restarted local API, and live-called `GET /api/v1/guides/a1111111-1111-1111-1111-111111111111/direct-booking`; the endpoint returned a created private experience on first call and reused the same experience ID on the second call.

- Re-ran full local validation after startup request:
  - `docker compose up -d` and `docker compose ps` => all infra services healthy.
  - Runtime checks: API (`8080`), admin (`3000`), provider (`3001`), and Expo web (`8081`) all responded.
  - Smoke suite passed: `make smoke`, `make smoke-api`, `make smoke-auth`, `make smoke-booking`.
  - Production builds passed: `npm run build` in admin and provider, `npx expo export --platform web` in mobile.
  - No reproducible runtime, typecheck, or build errors found in this pass.

- Investigated mobile white-screen issue on Expo web.
  - Root cause: web bundler crashed because `react-native-maps` (native-only internals) was imported through `MapScreen.tsx` -> `MoreScreen.tsx` -> `App.tsx`.
  - Confirmed by Metro error: importing `react-native/Libraries/Utilities/codegenNativeCommands` on web from `react-native-maps`.
  - Fixed by adding web-specific screen at `apps/mobile/src/screens/MapScreen.web.tsx` so web no longer loads native `react-native-maps` module.
  - Also aligned Expo package versions via `expo install` for `expo-location`, `react-native-maps`, and `react-native-webview`.
  - Verified with `npx expo export --platform web` (success) and `http://localhost:8081` responding `200 OK`.

- Investigated iOS mobile freeze on `iPhone 17` simulator after launching Expo Go.
  - Reproduced the crash during login screen mount with Metro/runtime error: `Client Id property iosClientId must be defined to use Google auth on this platform.`
  - Root cause: `apps/mobile/src/screens/LoginScreen.tsx` always initialized Expo's `useIdTokenAuthRequest` hook on iOS even when `EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID` was missing, so the login screen crashed before rendering and looked like a freeze.
  - Fixed by moving Google auth hook usage behind a dedicated `GoogleLoginButton` component that only renders when platform-specific Google OAuth config exists.
  - Added a safe fallback button/message so missing Google client IDs now show a clear configuration prompt instead of crashing the app.
  - Re-verified on the `iPhone 17` simulator: app now loads to the Huế Travel welcome screen without the previous iOS auth crash.

- Investigated mobile deployment/build blockers in the Expo app.
  - Confirmed the repo was missing `apps/mobile/eas.json`, while `apps/mobile/app.json` still contained `extra.eas.projectId` as an empty string.
  - Added `apps/mobile/eas.json` with `development`, `preview`, and `production` build profiles.
  - Removed the empty Expo `projectId` from `apps/mobile/app.json` so EAS can be linked cleanly later instead of inheriting a broken empty value.
  - Added `eas-cli` to `apps/mobile/package.json` devDependencies plus scripts for `doctor`, `prebuild:*`, and `build:*`, so the repo uses the correct CLI package instead of the unrelated npm package `eas`.
  - Verified the local binary works: `apps/mobile/node_modules/.bin/eas --version` returned `eas-cli/18.3.0`.
  - Confirmed remaining deploy blockers are environment-level, not app-code-level:
    - `eas whoami` => `Not logged in`, so remote EAS builds cannot start until Expo auth is configured on this machine.
    - `pod` is not installed, so local iOS native builds still require CocoaPods setup first.

- Restarted the full local runtime after the user's startup request.
  - Recycled Docker infra with `docker compose down && docker compose up -d`; PostgreSQL, Redis, MinIO, and Meilisearch all came back healthy.
  - Restarted API, admin web, provider portal, and Expo dev servers.
  - Verified live endpoints:
    - API health at `http://localhost:8080/health`
    - Admin at `http://localhost:3000`
    - Provider portal at `http://localhost:3001`
    - Expo web at `http://localhost:8081`
  - Forced Expo LAN hostname with `REACT_NATIVE_PACKAGER_HOSTNAME=192.168.1.246` so phone testing uses `exp://192.168.1.246:8081` instead of a broken `127.0.0.1` URL.
