## 2026-03-29

- Chạy local stack thành công với `docker compose up -d postgres redis minio meilisearch`.
- Seed demo data bằng `make seed`.
- Chạy API tại `apps/api` thành công; `GET /health` trả `200` với PostgreSQL và Redis đều `connected`.
- Smoke checks pass:
  - `make smoke`
  - `make smoke-api`
  - `make smoke-auth`
  - `make smoke-booking`
- `go test ./...` trong `apps/api` pass toàn bộ package có test.
- Web admin (`http://localhost:3000`) đăng nhập được bằng `admin@huetravel.local / HueTravel123!`; các trang dashboard, users, experiences, bookings, reviews, guide-apps, support, moderation, settings, analytics, reports đều render.
- Phát hiện lỗi admin moderation: UI gọi `GET /api/v1/admin/stories` nhưng backend hiện chỉ có route `/api/v1/feed`, nên endpoint admin trả `404`.
- Provider portal (`http://localhost:3001`) đăng nhập được bằng `guide.demo@huetravel.local / HueTravel123!`; các trang dashboard, my-tours, bookings, calendar, revenue, profile đều render có dữ liệu.
- Mobile Expo web (`http://localhost:19006`) chạy được; traveler login bằng `traveler.mobile@huetravel.local / HueTravel123!`.
- Mobile đã kiểm tra thành công: welcome -> login -> home -> experience detail -> booking -> payment mock -> booking list.
- Phát hiện trên mobile web:
  - AI Guide input send bị overlay bởi nút chat nổi nên click gửi thủ công không được trong web layout.
  - Một số ảnh Unsplash bị chặn với `net::ERR_BLOCKED_BY_ORB`.
  - Payment flow mở thêm tab `sandbox.vnpay.vn` lỗi trong khi backend mock vẫn xác nhận booking thành công.
- Đã fix:
  - Thêm admin story endpoints `GET /api/v1/admin/stories` và `DELETE /api/v1/admin/stories/:id`; admin moderation đã list và delete story thành công.
  - Ẩn floating shortcuts trên tab AI của mobile để không che nút gửi; gửi tin nhắn AI trên Expo web hoạt động lại.
  - Payment screen chỉ mở `payment_url` khi payment chưa `success`; mock payment không còn bật tab VNPay lỗi.
  - Thay URL Unsplash chết trong seed/migration và cập nhật DB local để hết lỗi `ERR_BLOCKED_BY_ORB` cho ảnh trải nghiệm liên quan.
- Kiểm tra và sửa chat:
  - Phát hiện lỗ hổng quyền truy cập: user không thuộc room vẫn có thể đọc/gửi tin nhắn nếu biết `room_id`; đã chặn ở chat handler bằng kiểm tra participant membership trước `GET/POST /chat/rooms/:room_id/...`.
  - Trang admin support trước đó chỉ đọc chat, hiển thị tên phòng chưa chuẩn, không có composer trả lời; đã thêm send message, dùng `other_participant`, đảo thứ tự tin nhắn về cũ -> mới và auto scroll xuống cuối.
  - Mobile trước đó có API `getOrCreateRoom` nhưng UI không gọi; nút chat trong tab bạn bè không có `onPress`. Đã nối flow `Cộng đồng -> Bạn bè -> 💬` để mở/tạo direct room và gửi tin nhắn trực tiếp.
  - Verify runtime sau fix:
    - Guide không còn đọc/gửi vào room traveler-admin; API trả `403 HT-AUTH-003`.
    - Admin web support gửi tin nhắn được từ UI.
    - Mobile Expo web mở room từ friend list và gửi tin nhắn thành công.
    - Đã xoá các room/message test tạo trong lúc verify khỏi DB local sau khi kiểm tra xong.
- Đã hoàn thiện realtime chat:
  - Backend giờ push message trực tiếp tới participant và hub WebSocket cho phép nhiều connection đồng thời trên cùng một user, nên socket global của app và socket trong màn chat không còn đá nhau.
  - Mobile `App.tsx` mở socket global khi đang ở main app và chat panel đóng; badge 💬 được cập nhật live ngay khi có tin đến mới mà không cần mở chat.
  - Mobile `ChatScreen` và web admin support đều mark read ngay khi mở room có unread, nên badge/unread count tự về `0` sau khi xem.
  - Verify runtime:
    - Khi traveler đang ở màn hình chính, admin gửi tin nhắn thì badge chat nổi tăng lên `1` ngay lập tức.
    - Mở room rồi quay lại thì badge biến mất ngay, không còn treo unread cũ.
    - Đã xoá room test realtime khỏi DB local sau khi kiểm tra xong.
- Tiếp tục UAT social/trips trên mobile và phát hiện thêm 3 lỗi client-side:
  - Feed stories từ backend trả `author_id/author_name/media_urls/location_name`, nhưng mobile đang đọc `user_id/user.full_name/image_urls/location`, nên tên tác giả hiện `Người dùng` và nút `Kết bạn` bắn request lỗi vì `user_id` bị `undefined`.
  - Danh sách bạn bè/pending từ backend trả `id`, còn mobile đòi `friendship_id`, nên nút `Chấp nhận` gọi `/friends/undefined/accept`.
  - `ChatScreen.sendMessage()` append message ngay sau API response mà không dedupe; khi WebSocket broadcast về gần như đồng thời thì cùng một `message.id` bị render 2 lần.
- Đã fix mobile:
  - Thêm normalize cho `Story`, `StoryComment`, `FriendInfo` trong `apps/mobile/src/services/api.ts`.
  - Đổi các nút icon-only trong tab bạn bè sang có chữ: `💬 Nhắn tin`, `Chấp nhận`, `Từ chối`.
  - Thêm dedupe theo `message.id` trong `apps/mobile/src/screens/ChatScreen.tsx` để hết trùng tin nhắn khi gửi.
- Đã fix thêm:
  - Thêm alias backend `GET /api/v1/trips/me` để trả cùng dữ liệu với `GET /api/v1/trips`; trước đó route này rơi vào `/:id` và trả `HT-VAL-001`.
  - Mobile `Trips` giờ load `members` từ `GET /trips/:id` và render trạng thái mời trong panel:
    - `Đã tham gia` nếu member status là `accepted`
    - `Đã mời` nếu member status là `invited`
    - disable nút để tránh bấm mời lặp.
- Verify runtime sau fix:
  - Feed hiển thị đúng tên tác giả và `Kết bạn` đổi trạng thái sang `Đã gửi`.
  - Tạo traveler phụ `codex.uat.1774722829@huetravel.local`, bơm lời mời kết bạn tới `traveler.mobile`, accept ngay trên UI tab `Bạn bè -> Lời mời`, rồi mở direct chat từ friend list.
  - Direct chat gửi 2 tin nhắn liên tiếp không còn bị render trùng.
  - Tạo trip UI `Codex UAT Trip 0138` với ngày `2026-04-05 -> 2026-04-07`, mời friend phụ và `Guide Demo Huế` từ chính UI `Trips`.
  - Friend và guide accept qua API, sau đó cả hai đều thấy room group `Codex UAT Trip 0138` và gửi tin vào room thành công.
  - Traveler mở `💬 Chat nhóm` từ card trip và thấy đúng 2 tin từ friend/guide; gửi thêm 1 tin từ traveler thành công.
  - Đã xoá toàn bộ dữ liệu UAT vừa tạo khỏi DB local: trip `Codex UAT Trip 0138`, direct room test, group room test, friendship test, và user phụ `codex.uat.1774722829@huetravel.local`.
  - `GET /api/v1/trips/me` trả `200` với danh sách trip hiện tại.
  - Trên UI `Trips -> Codex Secure Trip`, panel `Mời bạn` hiển thị `Codex Verify` là `Đã tham gia`, còn panel `Mời guide` hiển thị `Guide Demo Huế` là `Đã mời`.
- Đã chuẩn hoá reset DB local:
  - Thêm `scripts/migrate_all.sh` để apply đủ migration schema hiện dùng: `001`, `003`, `004`, `005`, `006`, `008`.
  - Thêm `scripts/reset_dev_db.sh` và đổi `make db-reset` thành full rebuild local dataset: drop schema -> migrate -> xoá seed baseline từ migration -> load `scripts/seed.sql` + `scripts/seed_real_data.sql`.
  - Giữ `make seed` cho seed demo auth tối thiểu và thêm `make seed-real` cho dataset lớn.
- Đã sửa `apps/api/migrations/007_comprehensive_seed.sql` để category khớp enum hiện tại (`sightseeing` / `experience` / `tour` / `food`) thay vì các giá trị cũ như `heritage`, `adventure`, `temple`, `market`, `restaurant`, `viewpoint`.
- Đã chạy `make db-reset` thành công:
  - DB local sau reset có `12` users, `11` experiences, `5` stories, `6` events, `0` trips.
  - API khởi động lại sạch, sync Meilisearch thành công.
- Verify sau reset:
  - `go test ./...` trong `apps/api` pass.
  - `make smoke-api` pass.
  - `make smoke-auth` pass.
  - `make smoke-booking` pass.
  - `./node_modules/.bin/tsc --noEmit -p tsconfig.json` trong `apps/mobile` pass.
- Sau smoke đã xoá lại toàn bộ user/booking `smoke-*` để DB vẫn giữ trạng thái seed sạch.
- Recheck cuối cùng sau cleanup:
  - `users=12`, `smoke_users=0`, `experiences=11`, `stories=5`, `events=6`, `trips=0`, `bookings=6`.
- Rà thêm web/provider sau reset:
  - `npm run build` trong `apps/web` pass.
  - `npm run build` trong `apps/provider-portal` pass.
  - Đã thêm alias CSS vars ở admin/provider (`--card-bg`, `--bg-secondary`, `--text-primary`) để các màn filter, modal, form không còn dùng biến chưa khai báo.
  - Đã sửa admin dashboard: 3 quick-action cards cuối trang giờ thực sự điều hướng sang `settings` thay vì chỉ hiện con trỏ tay.
  - Đã sửa provider profile: bấm `Huỷ` khi đang chỉnh sửa sẽ reload lại dữ liệu gốc từ server thay vì giữ form bẩn trong state.
- Đã hoàn thiện notifications cho web admin và provider portal:
  - `apps/provider-portal/src/lib/api.ts` và `apps/web/src/lib/api.ts` giờ có `getNotifications`, `getUnreadNotificationCount`, `markNotificationRead`.
  - Bell trên topbar của provider/admin không còn là placeholder; giờ mở dropdown, hiển thị unread, mark-one, mark-all, và poll lại mỗi 30 giây.
  - Verify API bằng login thật:
    - `guide.demo@huetravel.local` gọi `/api/v1/notifications` trả `{ success: true, total: 0, unread: 0 }`.
    - `admin@huetravel.local` gọi `/api/v1/notifications` trả `{ success: true, total: 0, unread: 0 }`.
  - `npm run build` sau patch pass cho cả `apps/provider-portal` và `apps/web`.
- Đã hoàn thiện persistence thật cho `settings` admin:
  - Thêm migration `009_admin_settings.sql` tạo bảng `admin_settings`.
  - Thêm backend `GET/PUT /api/v1/admin/settings` để load/save cấu hình admin vào DB.
  - `apps/web/src/app/settings/page.tsx` không còn dùng localStorage; giờ load/save từ server, hiển thị lần lưu gần nhất, và đổi cảnh báo thành “lưu tập trung vào DB nhưng runtime vẫn đọc env khi khởi động”.
  - `go test ./...` trong `apps/api` pass.
  - `npm run build` trong `apps/web` pass.
  - Verify runtime bằng instance tạm trên `18080` rồi restart lại API chính ở `8080`; login admin và `GET /api/v1/admin/settings` trả `200` với payload mới.
  - Đã xoá lại row test `ai_temperature` khỏi bảng `admin_settings` để DB local vẫn sạch.
- Đã sửa tiếp mobile `SettingsScreen`:
  - Thêm `api.deleteAccount()` để gọi `DELETE /api/v1/me`.
  - Nối các mục link chết `Điều khoản`, `Chính sách`, `Đánh giá ứng dụng` sang `Linking.openURL(...)`.
  - Nút `Xoá tài khoản` giờ hiện confirm dialog, gọi backend thật, rồi đăng xuất khỏi app.
  - `pushService.initialize()` trước đó chưa từng được gọi ở app startup; đã thêm init khi user vào main app.
  - Toggle `Push Notifications` giờ gọi `pushService.setEnabled(...)` thật và nếu bật lại thì re-init service.
  - `./node_modules/.bin/tsc --noEmit -p tsconfig.json` trong `apps/mobile` pass.
  - Verify API self-delete bằng user test `codex.settings.*@huetravel.local`: `DELETE /me` trả success, `GET /me` sau đó trả không còn user; đã xoá row test khỏi DB local.
- Đã làm thêm cho mobile settings để bớt “giả”:
  - `Language` và `Cache` là các hàng có thể bấm nên giờ hiển thị disclosure arrow đúng.
  - `Sessions` không còn hardcode `2` mà hiển thị `1` thiết bị cho session hiện tại.
  - `Cache Size` không còn số giả `24.5 MB`; giờ đọc stats thật từ `offlineCache` và bấm vào sẽ dọn cache cục bộ trong phiên.
  - `./node_modules/.bin/tsc --noEmit -p tsconfig.json` trong `apps/mobile` tiếp tục pass sau patch.
- Đã thêm `Provider Settings` thật trong portal:
  - Sidebar provider giờ có mục `⚙️ Cài đặt`.
  - Màn settings mới dùng dữ liệu thật từ notification API và password API:
    - xem unread count / danh sách thông báo gần đây
    - refresh / mark all read / mark từng thông báo
    - đổi mật khẩu guide ngay trong settings
  - Thêm browser alerts preference cho portal và hook vào polling notifications ở `apps/provider-portal/src/app/page.tsx`; nếu bật và trình duyệt đã cấp quyền thì notification mới sẽ hiện browser notification.
  - Logout trong provider portal giờ reset luôn notifications state và active page.
  - `npm run build` trong `apps/provider-portal` pass sau patch.
  - Thử check runtime qua Playwright nhưng tool launch bị vướng session Chrome local hiện có; build vẫn sạch và API `GET /api/v1/notifications` cho guide trả `200`.
- Đã hoàn thiện hot-apply runtime cho `admin settings`:
  - `apps/api/internal/handler/admin.go` giờ validate `ai_temperature`, `ai_max_tokens`, `vnpay_url`, `meili_url` trước khi lưu.
  - Sau mỗi `PUT /api/v1/admin/settings`, backend sẽ load lại full settings từ DB và apply ngay vào các service đang sống:
    - `AITripPlannerService`
    - `VNPayService`
    - `NotificationService`
    - `SearchService`
    - `FileUploadService`
  - `apps/api/internal/app/container.go` cũng gọi `ApplyStoredSettings(...)` lúc boot để settings đã lưu trong DB vẫn có hiệu lực sau restart.
  - Các service `AI`, `VNPay`, `FCM`, `Meilisearch`, `MinIO` đã thêm `UpdateConfig(...)` và snapshot thread-safe để tránh data race khi config đổi giữa lúc request đang chạy.
  - `apps/web/src/app/settings/page.tsx` đổi note mô tả: settings giờ áp dụng ngay lên runtime, không còn chỉ là DB draft.
- Verify hot-apply runtime:
  - `go test ./...` trong `apps/api` pass.
  - `npm run build` trong `apps/web` pass.
  - Restart API một lần để nạp code mới, sau đó verify không restart thêm giữa hai lần đổi config:
    - Login admin `admin@huetravel.local`.
    - `PUT /api/v1/admin/settings` set `vnpay_tmn_code=CODEXHOT1`, `vnpay_hash_secret=codex-secret-a`, `vnpay_url=https://example.com/hot-apply-a`.
    - Tạo traveler/booking/payment mới và nhận `payment_url` bắt đầu bằng `https://example.com/hot-apply-a?...`.
    - `PUT /api/v1/admin/settings` đổi ngay sang `CODEXHOT2`, `codex-secret-b`, `https://example.com/hot-apply-b`.
    - Tạo booking/payment mới nữa trong cùng process API và nhận `payment_url` bắt đầu bằng `https://example.com/hot-apply-b?...`.
  - Sau verify đã cleanup:
    - xóa 2 bookings test `13b5ddf7-c287-4b2f-abdb-3e780c89ba62`, `2b1cec5c-5b40-4d53-a1a0-c249d37f8fff`
    - xóa 2 user `codex.hotapply.*@huetravel.local`
    - xóa 3 row tạm trong `admin_settings` (`vnpay_tmn_code`, `vnpay_hash_secret`, `vnpay_url`)
    - restart lại API để runtime quay về trạng thái sạch theo env/default
  - Check cuối:
    - `admin_settings_count=0`
    - `hotapply_users=0`
    - API local đang chạy lại trên `http://localhost:8080`
- Đã rà và chốt lại flow `traveler` + `guide/provider` trên DB sạch:
  - Tìm ra bug thật ở `PUT /api/v1/me`: nếu client không gửi `email` thì backend đang ghi `email = NULL`, làm tài khoản guide demo mất email sau khi cập nhật hồ sơ và không đăng nhập lại được.
  - Đã sửa `apps/api/internal/handler/auth.go` theo semantics:
    - field không gửi lên => giữ nguyên giá trị hiện có
    - `bio` / `avatar_url` gửi chuỗi rỗng => xoá về `NULL`
    - `email` gửi rỗng => trả lỗi validation
  - Đã thêm test hồi quy trong `apps/api/internal/handler/handler_test.go` cho:
    - preserve email khi omitted
    - clear bio khi gửi chuỗi rỗng
  - Đã cập nhật `apps/api/internal/testutil/mocks.go` để sync lại `EmailIndex` khi update profile.
  - Đã cập nhật `apps/mobile/src/screens/ProfileScreen.tsx` để khi user xoá bio thì app gửi chuỗi rỗng thật, không còn gửi `undefined`.
- Verify sau fix:
  - `go test ./...` trong `apps/api` pass.
  - `./node_modules/.bin/tsc --noEmit -p tsconfig.json` trong `apps/mobile` pass.
  - UAT runtime trên DB sạch:
    - guide login bằng `guide.demo@huetravel.local / HueTravel123!` pass
    - guide update profile qua `PUT /me` rồi login lại vẫn pass
    - guide update guide-profile pass
    - guide create/update experience pass và xuất hiện lại trong `GET /experiences?guide_id=<guide_id>`
    - traveler register/login pass
    - traveler update profile + clear bio pass
    - traveler list `tour` experiences, toggle favorite, xem top guides pass
    - traveler direct-booking với `guide.demo` pass
    - `POST /payment/create` trong mock mode trả `status=success` và booking tự chuyển sang `confirmed`
    - guide nhìn thấy booking ở `GET /bookings/guide/me?status=confirmed`, complete được
    - traveler nhìn thấy booking ở `GET /bookings?status=completed`, tạo review được
  - Lưu ý hành vi hiện tại của local mock payment:
    - mock payment auto-confirm booking ngay trong `POST /payment/create`
    - vì vậy ở local/dev, guide flow đúng là `confirmed -> complete`, không phải `pending -> confirm`
- Sau khi verify đã chạy `make db-reset` lần cuối để xoá toàn bộ dữ liệu UAT và đưa DB local về seed sạch.
- Check cuối sau cleanup:
  - `users=12`, `experiences=11`, `trips=0`, `bookings=6`
  - login lại được cho `admin@huetravel.local`, `traveler.mobile@huetravel.local`, `guide.demo@huetravel.local`
  - API local đang chạy lại trên `http://localhost:8080`
- Đã bổ sung UI call cho mobile chat và chỉnh backend realtime call:
  - `apps/mobile/src/screens/ChatScreen.tsx`
    - thêm nút `audio/video call` trong header chat
    - thêm card trạng thái cuộc gọi trong room: incoming/ringing/ongoing, answer/decline/end/leave
    - sync `active_call` + participants từ REST và WebSocket
    - join/leave room qua WebSocket để sẵn cho signaling/call events
  - `apps/mobile/App.tsx`
    - thêm incoming call banner toàn app khi chat đang đóng
    - answer từ banner sẽ mở đúng room chat và join cuộc gọi
  - `apps/mobile/src/services/api.ts`
    - thêm type `CallSession`, `CallParticipant`
    - hoàn thiện các method `initiateCall/getActiveCall/leaveCall/getCallParticipants/getCallHistory`
  - `apps/api/internal/handler/reaction_call.go`
    - call events (`call_incoming/call_accepted/call_declined/call_ended/call_leave`) giờ push trực tiếp tới participant qua `hub.SendToUser`, không còn phụ thuộc room WS membership như trước
- Verify sau patch call:
  - `./node_modules/.bin/tsc --noEmit -p tsconfig.json` trong `apps/mobile` pass
  - `go test ./...` trong `apps/api` pass
  - restart API local để nhận code mới
  - smoke REST pass cho direct call:
    - `POST /calls/rooms/:room_id/call` -> `ringing`
    - `GET /calls/rooms/:room_id/active` thấy caller + invited participant
    - `POST /calls/:id/answer` -> active call chuyển `ongoing`
    - `POST /calls/:id/end` -> active call biến mất (`active_call=null`)
- Đã nối media call thật cho mobile theo hướng `react-native-webrtc`:
  - cài `react-native-webrtc@124.0.7` và `@config-plugins/react-native-webrtc@13.0.0`
  - cập nhật `apps/mobile/app.json`:
    - thêm `NSMicrophoneUsageDescription`
    - thêm permission Android `RECORD_AUDIO`
    - thêm plugin `@config-plugins/react-native-webrtc`
  - thêm service `apps/mobile/src/services/webrtc.ts` để load module WebRTC an toàn và fallback rõ ràng trên web/Expo Go
  - cập nhật `apps/mobile/src/screens/ChatScreen.tsx`:
    - direct call 1:1 giờ có WebRTC signaling thật qua WS (`call_ringing`, `call_offer`, `call_answer`, `ice_candidate`)
    - caller/callee tạo `RTCPeerConnection`, local media stream, remote stream
    - thêm full-screen media modal cho audio/video call
    - thêm mute, toggle local video, hangup
    - group call vẫn chỉ là call-state/session, chưa có media mesh/SFU thật
  - cập nhật `apps/mobile/App.tsx`:
    - incoming call banner toàn app vẫn hoạt động, answer từ banner mở room để join media call
- Verify sau patch WebRTC:
  - `./node_modules/.bin/tsc --noEmit -p tsconfig.json` trong `apps/mobile` pass
  - `npx expo config --json` trong `apps/mobile` pass
  - lưu ý test media thật cần native dev build hoặc `expo run:ios/android`; Expo Go / web không chạy media call thật
- Đã rollback hướng `call` trên mobile theo yêu cầu chuyển về chat-only:
  - thay `apps/mobile/src/screens/ChatScreen.tsx` bằng bản chat thuần, bỏ toàn bộ UI/state/signaling/media cho audio/video call
  - `apps/mobile/App.tsx` bỏ incoming-call banner và bỏ xử lý `call_*` ở global chat socket; socket giờ chỉ phục vụ unread message
  - xoá `apps/mobile/src/services/webrtc.ts`
  - gỡ `react-native-webrtc` và `@config-plugins/react-native-webrtc` khỏi `apps/mobile/package.json` + `package-lock.json`
  - `apps/mobile/app.json` bỏ `NSMicrophoneUsageDescription`, `RECORD_AUDIO`, và plugin WebRTC
  - `apps/mobile/src/services/api.ts` xoá các type/method call không còn dùng
- Verify sau rollback chat-only:
  - `rg` trong `apps/mobile` không còn reference call/WebRTC nào ngoài emergency contact icon
  - `./node_modules/.bin/tsc --noEmit -p tsconfig.json` trong `apps/mobile` pass
  - `npx expo config --json` trong `apps/mobile` pass
- Đã chạy vòng test tổng thể cuối ngày:
  - `cd apps/api && go test ./...` pass
  - `cd apps/web && npm run build` pass
  - `cd apps/provider-portal && npm run build` pass
  - `cd apps/mobile && ./node_modules/.bin/tsc --noEmit -p tsconfig.json` pass
  - `make smoke` pass
  - `make smoke-api` pass
  - `make smoke-auth` pass
  - `make smoke-booking` pass
- Kiểm tra role-based bằng API:
  - traveler demo login ok, `GET /me`, `GET /chat/rooms`, `GET /trips/me` đều trả `200`
  - guide demo login ok, `GET /me`, `GET /bookings/guide/me`, `GET /notifications`, `GET /guides/top` đều trả `200`
  - admin login ok, `GET /admin/dashboard`, `GET /admin/settings`, `GET /admin/stories` đều trả `200`
- Kiểm tra runtime local:
  - admin web đang chạy trên `3000`, `curl -I http://localhost:3000` trả `200`
  - provider portal đang chạy trên `3001`, `curl -I http://localhost:3001` trả `200`
  - mobile Expo web đang serve ở `19007`
  - mobile iOS simulator boot được, app launch qua Expo Go ở `19008`
  - screenshot native đã chụp:
    - welcome: `/tmp/hue-travel-ios-after-close.png`
    - login: `/tmp/hue-travel-ios-after-start.png`
- Sau smoke tests đã chạy `make db-reset` để đưa DB local về seed sạch, rồi verify lại:
  - `make smoke-api` tiếp tục pass
  - login lại được cho `admin@huetravel.local`, `guide.demo@huetravel.local`, `traveler.mobile@huetravel.local`
  - snapshot DB sau reset: `users=12`, `experiences=11`, `stories=5`, `events=6`, `trips=0`, `bookings=6`
- Giới hạn test còn lại:
  - chưa tự động đi sâu login/chat trong iOS simulator do nhập text tự động vào Simulator không ổn định trong môi trường hiện tại
  - Playwright browser vẫn bị chặn bởi session Chrome local đang tồn tại nên chưa chạy được browser E2E trực tiếp cho web/provider
- Bổ sung lớp UI E2E bằng Safari automation để bù cho Playwright/Chrome:
  - phát hiện một lỗi runtime tạm thời trên `apps/web` sau khi chạy `next build` song song với `next dev`: trang admin trả `Cannot find module './778.js'`
  - xác định đây là dev-server state bị vỡ do `.next` bị build đè, không phải lỗi code; đã kill và restart lại `apps/web` (`3000`) và `apps/provider-portal` (`3001`), sau đó runtime ổn định lại
  - dùng `osascript` + JavaScript injection để đi qua login form React thật:
    - admin Safari login thành công, body text hiển thị sidebar + dashboard stats + bookings gần đây
    - provider Safari login thành công, body text hiển thị dashboard guide và trạng thái online
  - dùng Safari automation với Expo web (`19007`) để verify traveler app:
    - click được `Bắt đầu khám phá` từ welcome screen
    - bơm credential thật cho `traveler.mobile@huetravel.local`
    - vào được `Trang chủ` với danh sách experience, top guides, tab bar (`Trang chủ / Cộng đồng / AI Guide`)
- Đã hoàn thiện thêm lớp Traveler preferences:
  - thêm backend persistence `GET/PUT /api/v1/me/preferences` với bảng `user_preferences`
  - mobile `App.tsx` nạp preferences sau login/restore session và propagate xuống `Home`, `ExperienceDetail`, `Booking`, `Payment`, `More`, `Settings`
  - thêm hỗ trợ đổi `currency` thật (`VND/USD/EUR`) và format lại giá/khuyến mãi trên các màn traveler
  - `useTranslation` giờ persist locale cục bộ, còn `pushService` tôn trọng `chat/promotions` preferences khi hiển thị foreground notification
- Đã vá 2 bug Traveler còn sót:
  - backend `device_count` từ `GET/PUT /me/preferences` không còn trả `0`; tối thiểu luôn là `1` cho session hiện tại
  - `SettingsScreen` không còn hiển thị `region` tĩnh; giờ đọc đúng giá trị từ backend và có modal chỉnh sửa khu vực thật
- Verify Traveler runtime sau khi hoàn thiện:
  - `cd apps/api && go test ./...` pass
  - `cd apps/mobile && ./node_modules/.bin/tsc --noEmit -p tsconfig.json` pass
  - API test với traveler demo:
    - `GET /me/preferences` trả `device_count=1`
    - `PUT /me/preferences` lưu được `locale=en`, `currency=USD`, `region=Central Hue`
    - `GET /me/preferences` sau đó trả lại đúng dữ liệu vừa lưu
  - Safari automation trên Expo web xác nhận:
    - home screen chuyển sang `English + USD` với giá experience đổi sang `$`
    - `Profile -> Settings` mở được, hiển thị đúng `Language: English`, `Currency: $ USD`, `Region: Central Hue`, `Active Sessions: 1 devices`
    - modal `Region` render đúng với phần mô tả và nút `Save`
- Đã reset traveler preferences về mặc định để giữ local demo sạch:
  - `locale=vi`
  - `currency=VND`
  - `region=Hue, Vietnam`
  - `push/email/chat=true`, `promo=false`
  - reload lại Expo web và xác nhận home screen quay về `Tiếng Việt + VNĐ`
- Đã test Traveler trên máy Android thật `SM_A217F` qua `adb`:
  - thiết bị lên serial `R58N53KZHYY`
  - vì Mac local chưa có Android SDK (`ANDROID_HOME` trống), không build native debug trực tiếp được; chuyển sang phương án `Expo Go`
  - tải và cài `Expo Go 54.0.7` chính thức từ Expo lên máy bằng `adb install`
  - cấu hình `adb reverse` cho `8081` (Metro) và `8080` (API), rồi chạy `expo start --go --localhost --port 8081` với `EXPO_PUBLIC_API_URL=http://127.0.0.1:8080/api/v1`
  - mở app trên máy bằng `exp://127.0.0.1:8081`
- Verify runtime trên Android thật:
  - app lên đúng splash/welcome của Huế Travel
  - đi qua `Bắt đầu khám phá`
  - đăng nhập thành công bằng `traveler.mobile@huetravel.local / HueTravel123!`
  - vào được home `Khám phá Huế`
  - dismiss popup lưu mật khẩu của Google và app vẫn hoạt động bình thường
  - mở được detail của experience `Đêm Nhạc Cung Đình trên Sông Hương`, thấy đúng title, guide `Hoàng Dũng`, giá `450.000₫`, nút `Đặt ngay`
- Ghi nhận khi test Android:
  - Metro log không có JS runtime error; chỉ có warning bình thường `Push disabled in Expo Go` do Expo Go không hỗ trợ push từ SDK 53+
  - native build/debug trên Mac này vẫn bị chặn bởi thiếu Android SDK path, nhưng test Traveler bằng Expo Go trên máy thật đã chạy được
- Điều tra lỗi Android `Không thể kết nối server`:
  - tái hiện được lỗi ở màn `Đặt chỗ`; screenshot `/tmp/hue-travel-android-current.png` cho thấy banner đỏ `Không thể kết nối server`
  - tại thời điểm lỗi, `adb reverse --list` không còn map `tcp:8080`, nên app đang trỏ về loopback `127.0.0.1:8080` nhưng mất tunnel tới Mac
  - rebind lại `adb reverse tcp:8080 tcp:8080` rồi refresh màn `Đặt chỗ` thì API log nhận `GET /api/v1/bookings status=200`, banner lỗi biến mất và UI về trạng thái `Chưa có booking nào`
- Đã vá root cause ở mobile dev API resolution:
  - cập nhật `apps/mobile/src/services/api.ts` để nếu `EXPO_PUBLIC_API_URL` là loopback nhưng Expo đang chạy trên host LAN, app sẽ ưu tiên lấy host từ `Constants.expoConfig?.hostUri` / `Constants.linkingUri`
  - logic mới dùng `http://<expo-host>:8080/api/v1` trong `__DEV__` thay vì bám cứng `localhost`, nên máy Android thật không còn phụ thuộc `adb reverse 8080`
  - verify lại bằng cách bỏ toàn bộ reverse `8080`, chạy Metro `--lan`, mở lại app trên máy và quan sát API log:
    - request từ traveler đi vào `/me`, `/me/preferences`, `/experiences`, `/guides/top`, `/bookings`, `/chat/rooms`, `/notifications`, `/ws`
    - IP nguồn trên API log là IP LAN của điện thoại (`192.168.1.94`), không còn là `127.0.0.1`
  - UI thực tế sau fix:
    - home screen tải đầy đủ dữ liệu thật trong LAN mode (`/tmp/hue-travel-android-lan-01.png`)
    - `Đặt chỗ` mở được và tải `GET /bookings` thành công, không còn banner lỗi (`/tmp/hue-travel-android-bookings-lan.png`)
    - `Thông báo` mở được và tải danh sách thật (`/tmp/hue-travel-android-notifications-lan.png`)
    - `Tin nhắn` mở được và tải room list thật (`/tmp/hue-travel-android-chat-lan.png`)
  - `cd apps/mobile && ./node_modules/.bin/tsc --noEmit -p tsconfig.json` pass sau patch
- Test social/chat/trip flow ngày 2026-03-29:
  - ban đầu phát hiện bug thật: `POST /api/v1/trips/:id/accept` có thể trả `200` dù user chưa được mời, và handler vẫn `AddParticipant` vào group chat nếu repo không báo lỗi
  - nguyên nhân là `TripRepository.AcceptInvite/DeclineInvite` không kiểm tra `RowsAffected()`
  - đã vá ở backend:
    - `apps/api/internal/repository/trip_repository.go`: thêm `ErrTripInviteNotFound`, trả lỗi khi `RowsAffected() == 0`
    - `apps/api/internal/handler/trip.go`: map lỗi này thành `403 HT-AUTH-003` và chỉ `AddParticipant` sau khi accept thật sự thành công
  - verify lại bằng ca test mới:
    - user lạ gọi `POST /trips/:id/accept` khi chưa được mời -> `403`, room group vẫn chỉ có creator
    - sau khi kết bạn đúng payload `POST /friends/request { user_id }`, traveler mời member vào trip -> `200`
    - member accept trip -> `200`
    - traveler mời `guide.demo` vào trip -> `200`
    - guide accept trip -> `200`
    - direct chat giữa traveler và member hoạt động hai chiều
    - group chat hoạt động đủ 3 phía: traveler, member, guide
    - `GET /trips/:id` sau cùng trả đủ 3 `trip_members`: creator/member/guide đều `accepted`
  - `cd apps/api && go test ./...` pass sau khi vá
- Retest `chat nhóm + mời hướng dẫn viên` sau khi vá:
  - tạo traveler test mới, kết bạn với `traveler.mobile`, tạo direct room và xác nhận direct messages 2 chiều
  - tạo trip `Group Chat UAT`, invite member, member accept, invite `guide.demo`, guide accept
  - gửi 3 message trong room nhóm từ `member`, `guide`, `traveler`; `GET /chat/rooms/:room_id/messages` trả đủ cả 3 sender
  - `GET /trips/:id` trả đủ `trip_members` với 3 role: `creator`, `member`, `guide`, tất cả `accepted`
  - DB local sau test đã được reset sạch bằng `make db-reset`; không còn user `group.test.*` hay trip `Group Chat UAT`
  - lưu ý: reset DB làm các session/token đang mở trên app trở thành cũ; cần login lại nếu tiếp tục test trên thiết bị
- Dọn phần `calls` sau khi chốt sản phẩm theo hướng chat-only:
  - gỡ wiring `/api/v1/calls` khỏi `apps/api/internal/app/routes.go`, không còn public call endpoints trên local API
  - bỏ `CallRepo` / `CallH` khỏi DI container ở `apps/api/internal/app/container.go`
  - xóa nhóm endpoint Calls khỏi `apps/api/internal/handler/docs.go`
  - verify lại sau restart API: `GET /health` vẫn `healthy`, còn `GET /api/v1/calls/history` trả `404`
- Dọn sâu dead code của feature call:
  - xóa hẳn `apps/api/internal/repository/call_repository.go`
  - bỏ `CallRepo` khỏi `apps/api/internal/repository/interfaces.go`
  - đổi `apps/api/internal/handler/reaction_call.go` thành `apps/api/internal/handler/reaction.go`, chỉ giữ lại reaction handler
  - cập nhật `docs/RELEASE_CHECKLIST.md` để không còn item `Calls flow`
  - `rg` lại toàn repo không còn `CallHandler`, `CallRepo`, `CallRepository`, `/api/v1/calls` hay `chat/calls`
  - `cd apps/api && go test ./...` vẫn pass, `GET /health` vẫn healthy sau restart API lần cuối
- Dọn traveler settings để phản ánh đúng trạng thái app:
  - bỏ row `Dark Mode` khỏi `apps/mobile/src/screens/SettingsScreen.tsx` vì app hiện dùng dark theme cố định, không có switch theme thật
  - `cd apps/mobile && ./node_modules/.bin/tsc --noEmit -p tsconfig.json` pass sau khi dọn UI
